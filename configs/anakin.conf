server {
    listen 8080;
    host localhost;
    root ./test;
    index index.html;
	error_page 404 test/404.html;

    location / {
        root ./test;
        methods GET POST;
        index index.html;
        # client_max_body_size 10;
		upload_dir test/upload;
    }

    location /cgi-bin {
        root ./test;
        auto_index true;
        methods GET POST;
        cgi_extension .py;
        cgi_path /usr/bin/python3;
    }
}
